{
  "name": "xrpl.js-monorepo",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf node_modules",
    "dev": "turbo watch build",
    "lint": "turbo run lint",
    "sort-package": "turbo run sort-package-json",
    "sort-package-root": "npx sort-package-json",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@total-typescript/tsconfig": "^1.0.4",
    "prettier": "^3.5.3",
    "turbo": "^2.5.4"
  },
  "packageManager": "pnpm@10.12.0",
  "engines": {
    "node": ">=20"
  }
}
